"use client";

import React, { useState } from "react";
import Menus from "../Menus";

export default function MinhaPagina() {
  const [loading, setLoading] = useState(false);
  return <Menus>Minha Pagina</Menus>;
}
